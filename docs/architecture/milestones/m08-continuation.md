# M8 remaining implementation tasks

**Date:** 2026-09-08

**Authority:** The owner approved continuing the following tasks after Tasks1–2 merged: denoise/sharpen, shared orientation, and full pipeline composition. Work used `feat/m08-completion` in the retained isolated M8 worktree and merged through [PR #12](https://github.com/m4bulmagd/Lumora/pull/12) as `17d1ede`. Native Windows11 M4/M5 acceptance remains deferred; designated Windows reference/workstation evidence remains separately required.

## Completed baseline

CLAHE Task2 merged via [PR #11](https://github.com/m4bulmagd/Lumora/pull/11) as `c0c4102`. Linux/GCC13 and Windows/MSVC Debug/Release PR CI passed at `b85a626`; both post-merge [Linux](https://github.com/m4bulmagd/Lumora/actions/runs/34174289769) and [Windows](https://github.com/m4bulmagd/Lumora/actions/runs/34174289743) jobs also passed. Existing local evidence and review artifacts remain retained. Neither Task1 nor Task2 is being reimplemented as a fresh task.

## Task4 orientation contract

The standalone display transform is independent of Task3 and is implemented while detail-stage and executor preflight run. `OrientationTransform` accepts explicit image layouts, display storage and bounded byte spans, reports a tight output layout, and maps both Gray8 and Gray16 by exact integer coordinates. It applies horizontal flip, vertical flip, then clockwise rotation. Quarter-turns swap dimensions. Identity directly copies to a distinct destination; the executor can bypass it for an identity installation profile.

All validation precedes writes: supported rotation/storage pair, expected output dimensions, complete spans and no complete-payload aliasing. Odd strides and unaligned Gray16 samples are valid; only active sample bytes are copied. Input, padding and canaries remain unchanged. Stable `orientation_*` errors identify failures. No OpenCV dependency or heap allocation is needed on the successful transform path.

Task5 integrates the same immutable stopped-state orientation into both display routes and prepares the required display layouts/pool leases. Native RawFrame and enhanced U16 storage stay untouched. No new live orientation control or administrator profile UI is added in Task4.

## Task3 detail contract

Gaussian denoise gains trailing `sigma` with default zero (automatic coefficients); accepted explicit sigma is finite [0,5], kernels3/5/7. Median accepts kernels3/5 and sigma zero. These bounds are now explicit configuration decisions, not previously specified PRD values. Both direct factories and compiler enforce them.

All neighborhoods use REFLECT_101, including singleton dimensions. Pinned OpenCV prepares double Gaussian coefficients; Lumora performs separable convolution with a prepared double intermediate and one final U16 round. Median selects the middle of a fixed9/25-sample reflected neighborhood. These locally owned execution paths remove per-call backend allocation routes. Gaussian output is defined by this double-kernel calculation and can differ from OpenCV's quantized U16 Gaussian output; analytical references and future designated-platform comparisons must reflect this provenance.

Sharpen radius is sigma, kernel is `2*ceil(3*radius)+1`, blur is rounded once to U16, detail is signed original-minus-blur, and only absolute detail strictly above threshold contributes. Final output saturates and rounds positive halves upward. Amount0 and threshold65535 are exact copy paths after normal validation. Existing parameter bounds remain unchanged.

Prepared stages own scratch through an explicit factory/PImpl and belong to one sequential processing worker. This replaces the earlier plan's unavailable workspace-scratch access; the executor owns their lifetime and budgets their exact requested bytes. Gaussian/sharpen each retain one width×height double image and a bounded coefficient array. Median retains no full image scratch. Factories accept a configurable scratch budget (default256MiB per standalone stage); Task5 applies a separate complete resource budget and accounts for active/candidate/retired owners. No successful process call resizes storage or constructs a kernel.

The allocation gate for M8 measures processing of a preexisting immutable RawFrame through output FrameBundle publication and release, including metadata/control blocks and stage work. Acquisition-side metadata construction is a separate scope and will not be described as allocation-free by that measurement. Designated Windows reference/workstation acceptance remains pending external evidence; locally implementable allocation debt is not deferred to that gate.

## Task5 execution and resource decisions

Task5 is divided into four reviewable implementation slices: (5A) pooled output metadata and fixed timing records; (5B) bounded prepared CLAHE execution; (5C) complete activation/execution, resource admission, shared orientation and enhancement-failure fallback; (5D) canonical Standard, deterministic reference tooling, allocation verification and benchmark JSON. This sequencing preserves working ownership and algorithm tests while exposing failures at a small boundary.

Output metadata keeps the existing shared_ptr<const Frame> API. A prepared FrameObjectPool provides separate typed object slots and control-block slots, with runtime discovery of the standard library's actual allocator-rebound control shape. Weak references retain their old control slot until release; they never observe a reused object generation. Pool state outlives the engine whenever displayed/captured/weak owners remain. Existing allocating frame factories stay available; the engine uses new pooled overloads. Exhaustion never falls back to heap frame storage. Successful timing records use sixteen inline entries with owned identifiers of at most63 bytes; checked append rejects overflow or invalid names without truncation.

An opaque immutable FrameObjectPoolPlan performs only bounded control-shape probes and computes storage requirements. Bulk pool creation consumes that same plan after aggregate admission; it does not allocate capacity-sized slabs merely to discover their size. Reported required storage includes aligned object/control slabs, requested free-list/bookkeeping storage and documented fixed state. Probe and allocator overhead coverage is explicit.

The CLAHE wrapper's public contract and U16 references remain, but Task5 replaces its per-apply OpenCV state with a prepared CPU implementation of the pinned histogram clipping, redistribution, reflection and interpolation arithmetic. It retains a LUT, one sequential histogram and bounded interpolation/reflection indices. No OpenCV scheduler/backend call runs per frame. Any copied upstream implementation retains its license/copyright and provenance. Compatibility is established by analytical cases, the existing fixture and targeted comparisons, not asserted from intent. This resolves the known allocation gap rather than treating it as an external Windows gate.

Prepared CLAHE preserves pinned float saturation using the private inline OpenCV helper. Preparation and execution require the normal `FE_TONEAREST` rounding environment; another standard rounding mode is rejected before scratch or output writes, without changing the caller's environment. Its image-count scratch trait becomes zero after removing the bridge images, while an explicit byte estimator still accounts for LUT, histogram, interpolation and reflection arrays. Resource admission must use those bytes.

The default safe preparation budget is512MiB of explicitly accounted Lumora resource storage, configurable at engine/session preparation. It includes aligned pixel pools, output object/control slabs, orientation scratch, stage buffers and bounded configuration/activation reserve. It is an admission budget, not total process RSS or a promise about operating-system/dependency memory. Required bytes and the selected limit are exposed before streaming; impossible arithmetic or an insufficient limit rejects preparation before acquisition. Candidate configuration preparation is serialized; executable snapshots stay private, with at most active, candidate and one in-flight retired configuration. Resource accounting must cover retained stage state and last-gamma cache without unbounded historic caches.

Candidate immutable stage configuration may be prepared on the calling control thread using separate owned resources while the worker processes its prior snapshot. Publication is atomic after successful validation/preparation. This clarifies earlier stopped-stage wording: existing scratch/pixel pools are never resized by process or concurrent activation; source extent/storage, orientation and session resource-envelope changes still require stopped-state replacement. Shared unchanged stage handles are accessed sequentially by the single worker; preparation must not warm or mutate an active backend. Unchanged gamma retains one cached table across unrelated revisions and disable/re-enable; changed gamma creates a new table before publication.

Preparation reserves a fixed configurable per-activation storage envelope for each of the three possible executable owners. Its automatic default divides the remaining session budget by three after subtracting fixed storage and the bounded gamma-cache reserve, with checked arithmetic. This is reserved admission headroom, not a claim that all reserved bytes are resident or allocated. A live candidate exceeding the envelope is rejected before allocation and leaves the active configuration intact; increasing the envelope requires stopped preparation. Diagnostics expose fixed storage, the three-owner reserve, candidate requirements and the selected limit. New activation/stage owner controls use checked preparation reserves enforced before actual allocation, alongside exact object/PImpl and scratch payload accounting. Persistent error strings and transient compiler/error diagnostics are excluded from this prepared-resource envelope; status keeps one immutable diagnostic handle without a history. This limit therefore does not cap diagnostic or total process memory.

The budget covers one prepared session. Immutable displayed or captured frames may retain pools from prior sessions beyond this envelope; the limit is not a bound on all outstanding sessions or total process memory. Cross-session retention policy remains unchanged.

Execution preserves canonical subsequences accepted by M7; the default registry and Standard definition contain all eight canonical entries. Normalize into A, evaluate Original window/level into B, map Original before overwriting B, then ping-pong Enhanced through enabled stages. Missing WindowLevel retains the established identity Original mapping. Disabled Enhanced stages are not invoked; Original-only WL work is counted separately. Both display routes receive the same immutable orientation; native raw and Enhanced U16 stay unchanged. Nonidentity orientation uses one dedicated native display temporary reused sequentially and counted separately from publishable pool leases.

After three consecutive enhancement failures, publish valid Original-only output and retain a non-modal warning until a valid new configuration or explicit processing retry is consumed. The first two failures discard their processed frames; successful enhancement before the threshold resets the consecutive count. A partial Enhanced image is never published. Failure of normalization, Original WL/map or orientation cannot supply a valid fresh Original; it drops the frame and leaves existing stale indications truthful. Structured processor status must preserve the latched warning through Original-only success instead of letting the worker clear it as ordinary enhancement recovery. Deterministic stage-failure/invocation testing uses a narrow internal factory/dispatch seam.

Enhanced-only terminal map/orientation faults join that threshold after Original is complete; Original-route faults cannot publish fallback. Intervening resource/source/Original faults neither increment nor reset the count of failed enhancement attempts. A warning-local Retry enhancement button sends a coalesced, generation-checked processing intent consumed at a frame boundary. It does not reconnect the camera or introduce the M9 configuration editor, and its warning coexists with existing pause/stale status.

## Standard and evidence contract

Standard is the complete ordered definition: Normalize enabled; WindowLevel enabled at65535/32767.5; BrightnessContrast enabled at0/1; Gamma enabled at1; CLAHE enabled at clip2/grid8; Gaussian Denoise enabled at kernel3/sigma0; Sharpen enabled at amount1/radius1/threshold0; Invert disabled. The normalized settings comparison ignores runtime configurationRevision only. Existing identity startup defaults remain available; no M9 preset/editor UI is added here, and benchmark settings may not drift from the shared Standard factory.

Reference tooling creates fixed integer-defined U16 ramp, gradient, edge, noise and nonsquare orientation input, writes standard big-endian PGM data and records algorithm/source/platform provenance. Existing committed expectations are never regenerated by a passing test. A Windows capture command produces candidate artifacts for review. A schema-valid workstation record explicitly says pending designation, with unavailable facts null; it cannot be used to claim acceptance. Per-algorithm maximum error and image-difference thresholds remain unapproved until designated Windows outputs are captured and reviewed.

Benchmark and reference command-line tools may use the existing Qt Core dependency privately for JSON, hashing and host metadata. Processing and core libraries remain independent of Qt. The optional tools must build with tests disabled, run without a GUI platform plugin, and preserve source/backend provenance in every artifact.

Benchmarks measure each prepared stage and full Standard through paired display mapping/orientation and immutable output publication/release at512²,1024²,2048², using100 warm-up and at least500 measured frames. They report median, nearest-rank P95, measured wall-time FPS, allocations/count/bytes and measurement coverage, working-set samples, input/configuration/backend identity and errors. Input/samples/retention slots are prepared before measurement; JSON output and working-set probes run outside the measured processing interval. Smoke mode has explicitly reduced counts/sizes and no workstation timing threshold. A separate allocation executable verifies1,000 same-size successful frames per identity/nonidentity profile, including output lifetime and positive-control allocations. Both benchmark and allocation executables measure their reported C++ allocation fields; their replacement operators remain outside application/GTest targets. Acquisition construction and error-string allocation remain explicitly outside its success-path scope.

Optional Linux allocation-only evidence may preload the installed glibc debug allocator and bracket the prepared region with tracing markers. Actual provider, C/C++/shared-library positive controls and an empty trace must pass before measurement. Completed per-region traces supplement replacement-new counters; they do not cover private/static allocators, direct mappings or Windows heaps. Traced runs are separate from performance timing, and unavailable tracing is never reported as zero allocation. This support belongs only to evidence tools, with no production debug-allocator linkage.

The designated Windows2048² Standard P95≤33.3ms and sustained30FPS acceptance cannot pass without a selected workstation and actual measurements. Local allocation debt and functional composition must nevertheless be implemented and tested now; only the external reference/workstation evidence remains pending.

## Task4 implementation checkpoint

Standalone orientation is implemented at `463468d` with ten focused tests. Coordinate assertions failed against a compile-ready placeholder, and four validation groups subsequently failed before validation was implemented. Incremental Linux Debug and Release builds passed, along with the focused orientation test and all ten Processing CTest entries in each configuration. Independent task review approved spec compliance and code quality with no findings. Windows CI and full branch integration remain later verification steps. Logs and report are retained under `out/qa/m08-task4/` and the M8 execution workspace.

## Task3 implementation checkpoint

Prepared denoise/sharpen is implemented at `4d05d52`. Independent task review approved spec compliance and code quality with no findings. Gaussian and median denoise, sharpening, compiler validation and scratch accounting pass focused and all twelve Processing CTest entries in both Linux Debug and Release. The separate allocation executable observes all eight C++ new forms in positive controls and zero calls over1,000 prepared executions of each detail stage. Its coverage is replacement-new traffic, with no claim about arbitrary C or DLL allocators.

A diagnostic2048² Release microprobe identified repeated row/border work. Hoisting that work while preserving tap accumulation order reduced the observed Gaussian3 average from381.768ms to56.161ms and radius1 sharpening from884.208ms to125.277ms, with unchanged checksums. These3-warm/10-measured standalone observations are provisional optimization evidence, not the full Standard benchmark or performance acceptance. Further performance work is expected during complete pipeline characterization. The report and exact logs remain under the M8 execution workspace and `out/qa/m08-task3/`.

## Task5A ownership checkpoint

Pooled output metadata and fixed timing records are implemented at `79e5f64`. Debug and Release each pass all24 affected Core/Processing registrations, including weak-owner lifetime, cross-thread release, partial-transaction rollback and preparation-allocation failure injection. Full application Debug and Release builds each pass48 headless checks and native X11 smoke1/1. Independent task review approved spec compliance and quality with no findings. Four focused frame/pool/timing/engine registrations also pass under AddressSanitizer, UndefinedBehaviorSanitizer and leak detection. The initial sandbox wrapper prevented leak scanning; running the same tests outside that wrapper passed without source changes. At the documentation checkpoint`8c5d627`, [Linux/GCC13.3 CI](https://github.com/m4bulmagd/Lumora/actions/runs/34182651674) passed48 headless plus one X11 check in each configuration, and [Windows/MSVC19.44 CI](https://github.com/m4bulmagd/Lumora/actions/runs/34182651687) passed48/48 in both. The completed branch still requires final review and exact-head CI before integration.

The existing engine allocation probe first observed12,000 allocations and734,000 requested bytes over1,000 successful publication/retention/release cycles. The prepared implementation records zero allocations, requested bytes and deallocations for the same cycle boundary in Debug and Release, after100 warm-ups. Raw creation, preparation, error diagnostics and external C/DLL heaps are outside this replacement-new measurement. Full enabled enhancement composition remains a later Task5C/5D check.

On local GCC15.2/libstdc++, the normal9 processed/16 display/9 bundle envelope uses34 discovered control slots,17,072 bytes of aligned slot storage and18,496 total accounted requested bytes. The opaque plan discovers platform control shapes before bulk admission; these local sizes are not hardcoded cross-platform assumptions. Required storage includes every capacity-scaled slab/free-index array and fixed plan/state/facade payload, excluding allocator headers and the three bounded preparation-owner shared_ptr controls. It is not process RSS. Exact logs and the implementation report are retained under `out/qa/m08-task5a/` and the M8 execution workspace.

## Task5B prepared CLAHE checkpoint

The native prepared CLAHE replacement is implemented at `6ffc627`, with public contract comments clarified in `2815db4`. Independent task review approved spec compliance and quality; its one Minor documentation suggestion is addressed and approved. Creation and processing require FE_TONEAREST, while pure sizing does not. The default standalone scratch budget is256MiB; retained array payload is786,528 bytes for4x4/grid2 and8,699,904 bytes for2048x2048/grid8, excluding fixed owner/allocator overhead.

Linux Debug and Release each pass123 processing cases, including19 CLAHE tests with independent clipping/redistribution/half-tie oracles and exact comparisons against pinned OpenCV4.12 across grids2/3/8/32. All13 Processing CTest registrations also pass in both configurations. The same19 CLAHE cases pass with address, undefined-behavior and leak sanitizers enabled outside the sandbox's ptrace wrapper.

The old backend completed1,000 valid calls while making10,004 C++ allocations and requesting1,050,792,160 bytes in total. The native implementation completes1,000 alternating odd-stride/unaligned calls with zero allocations, stable A/B checksums and intact input/padding/canaries; all10 measured preparation allocation failures are recoverable. This is prepared-stage replacement-new evidence. Full composition/publication and broader Linux allocator-event measurements remain subsequent checks; no designated Windows reference/performance acceptance follows from it.

The adapted source retains the complete NVIDIA/Itseez notice, also reproduced in `THIRD-PARTY-LICENSES/OpenCV-CLAHE.txt` and referenced by `NOTICE`. No current binary packaging route exists; future distribution must carry this file and the ordinary dependency notices. Exact logs and reports remain under `out/qa/m08-task5b/` and the M8 execution workspace. At `6ffc627`, [Linux/GCC13.3 CI](https://github.com/m4bulmagd/Lumora/actions/runs/34183915044) passed48 headless plus one X11 check in Debug and Release; [Windows/MSVC19.44 CI](https://github.com/m4bulmagd/Lumora/actions/runs/34183915092) passed48/48 in both. The new analytical and exact same-platform comparisons ran on both platforms. The old Linux-only fixture and designated-reference distinction remain documented; completed-branch verification is still required.

## Detail performance checkpoint

At `04215a9`, private horizontal interior and vertical row helpers process eight independent pixels while preserving each pixel's tap order, byte-safe access and rounding. Reflected borders and tails remain scalar. Prepared storage, public APIs and stage parameters are unchanged. On the local GCC 15.2 Release build, three short 2048-square probes reduced mean Gaussian3/auto time from 56.762 to 27.522 ms (51.51%) and sharpen1/1/0 from 124.578 to 78.165 ms (37.26%). Each probe used three warm-ups and ten measured calls; these figures are standalone engineering observations, not full Standard P95 or workstation acceptance.

Both complete 2048-square output pairs are byte-identical against the prior implementation. Debug and Release each pass 20 focused exactness cases and all 13 Processing registrations; the allocation probe retains eight positive controls and zero allocations across 1,000 prepared calls per stage. The added scalar references cover intermediate double bit patterns, complete pixels, borders/block boundaries/tails, odd and unaligned strides, tiny dimensions, kernel transitions and reuse. GCC emits packed horizontal and sharpen vertical arithmetic with separate multiply/add; Gaussian vertical remains eight independent scalar chains. Source snapshots, binaries, disassembly, full outputs and logs are retained under `out/qa/m08-task5-performance/`. Independent task review approved both spec compliance and code quality with no findings. At `51d01ad`, [Linux/GCC 13.3 CI](https://github.com/m4bulmagd/Lumora/actions/runs/34185945138) passed 48 headless plus one X11 check in Debug and Release; [Windows/MSVC 19.44 CI](https://github.com/m4bulmagd/Lumora/actions/runs/34185945102) passed 48/48 in both. These runs include the new exactness and allocation checks. Full Standard characterization and completed-branch verification remain required.

## Task5C composition and recovery checkpoint

Full execution and resource admission are implemented at `a71af97`, with exception attribution corrected in `637ca06`. Independent task review and the scoped correction review approve both spec compliance and quality; no findings remain open. The engine prepares enabled stages, reuses unchanged handles and the bounded gamma cache, publishes immutable activation snapshots, and processes both display routes with shared orientation. Session admission reports checked requested storage and rejects oversized preparation before bulk allocation. Its resource figures preserve the coverage and exclusions above, including prior-session retention and diagnostic strings.

After three enhancement failures, the pipeline publishes fresh Original-only frames and keeps a separate warning visible. Retry is generation checked, observable while idle, and consumed at the next frame; it does not reconnect acquisition. Tests cover partial failures, resource/Original-route classification, successful recovery, concurrent preparation/publication, retained old snapshots, gamma reuse, and candidate rejection preserving the prior configuration. Translated exceptions identify their stage or operation while retaining their original diagnostic detail and classification.

At `a71af97`, full local Debug/Release application builds each pass all 48 headless registrations and native X11 smoke. The diagnostic-only correction then passes all 31 engine cases in each configuration. At `637ca06`, four targeted pool/timing/engine registrations pass with AddressSanitizer, UndefinedBehaviorSanitizer and leak detection enabled, including the new preparation and ownership paths. Logs remain under `out/qa/m08-task5c/`. Linux/Windows checkpoint CI and completed-branch verification remain separate from these local results.

The expanded existing allocation regression covers four 8×8 full-enabled profiles with CLAHE grid2: identity/nonidentity orientation, each in healthy and latched Original-only mode. Every profile completes 100 warm-ups and 1,000 measured publication/retention/release cycles with zero replacement-new allocations, requested bytes and deallocations in Debug and Release. All 39 injected engine-preparation allocation failures clean up unpublished resources. These reduced fixtures establish those ownership paths; Task5D still supplies the distinct full Standard 64×48 allocation proof and normal-size characterization. No designated Windows reference or performance acceptance is implied.

## Task5C cross-platform verification

At `a4de229`, [Linux CI](https://github.com/m4bulmagd/Lumora/actions/runs/34193745358) passes 48 headless checks plus native X11 in Debug and Release. [Windows CI](https://github.com/m4bulmagd/Lumora/actions/runs/34193745459) passes 48/48 in Debug (45.79s) and Release (23.52s).

A deterministic MSVC Debug allocation-test hang was localized to the first injected allocation: iterator-debug `std::vector` construction allocates a proxy inside a `noexcept` constructor. The test now verifies this termination in an isolated child with a ten-second bound and required exit code86, then exhaustively injects failure into creation from an already admitted plan. It explicitly marks the complete compiler/planner allocation-failure sweep unexercised for iterator Debug. Windows Release and other builds retain the complete sweep and additionally test admitted creation. Debug flags, all four 1,000-cycle profiles, and the existing 180-second evidence timeout remain unchanged. Local Release covers 39 complete and 16 admitted creation failure sites. Passing CI executes these assertions; passing child output is not separately printed by the workflow. This correction does not resolve the distinct historical M7 intermittent context-retirement timeout.

## Task5D evidence-tool implementation checkpoint

Source `eebbbd0` adds the shared explicit Standard definition, three optional evidence executables, strict typed JSON artifacts and schemas, and fourteen independently derived exact reference cases. The candidate generator preserves committed expectations and labels backend/full-chain candidates provisional. Workstation designation and unapproved thresholds remain pending/null. Private QtCore support stays outside Core/Processing; only the two measurement executables link the replacement-new counters.

Focused Debug verification passes all three affected CTest registrations. Optional local glibc smoke verifies positive and empty controls and records no events in twenty measured cycles per orientation; it is explicitly reduced nonproof evidence. Independent task review, full application/tests-disabled builds and normal Release benchmark/allocation verification are still underway. No designated workstation or milestone acceptance is claimed.


At `eebbbd0`, full local application/tools builds pass without compiler warnings: Debug51/51 headless (180.92s), Release51/51 (23.33s), and native X11 1/1 each. A fresh tests-OFF/Basler-OFF/tools-ON Release graph builds the application and all three tools without GTest, then passes direct bounded smoke. [Linux CI](https://github.com/m4bulmagd/Lumora/actions/runs/34194651488) passes 51 headless plus X11 in each configuration. Root independently verifies all28 PGM hashes and the original derivation hash.

Review identified four evidence defects: Windows configuration collisions, unsafe compiler-option serialization, inconsistent reviewed-reference/workstation claims, and an incorrect adapted CLAHE source-license label. Corrections `515964c` and `633a36d` add C++-context provenance capture, escaped flags for every configured build type, strict case-ID-based approval/hash matching, and the retained NVIDIA/Itseez BSD source notice. The subsequent MSVC constant-condition warning is corrected without disabling warnings. Focused Release verification passes all four affected registrations; schemas reject contradictory reviewed states and all oracle bytes remain unchanged. The approval tests use explicitly synthetic temporary bundles, never acceptance evidence. Scoped re-review, matching Windows CI and normal Release characterization subsequently completed as recorded below.

## Normal Release characterization and allocation evidence

The normal evidence run used immutable binaries built from clean `1b85953f75eb61f9c755b492b4168b4b8549fa5c` (captured 2026-09-08T06:57:28Z), GCC 15.2 Release `-O3 -DNDEBUG`, Qt 6.11.1 and OpenCV 4.12.0/port 9 on the Ubuntu 26.04 i7-10510U laptop (8 logical cores). No local builds or other heavy validation ran alongside it. A later source-only Windows portability correction, `73eb61f`, changes the premeasurement environment guard; it did not replace these measured binaries or change the measured pipeline. Artifact provenance remains the actual clean build revision.

All 33 rows (11 per 512/1024/2048 extent) complete 100 warm-ups and 500 measured frames, with no processing errors, drops, or measured C++ allocation calls, bytes or releases. The complete Standard results are:

| Extent | Identity P95 / FPS | Horizontal flip + clockwise 90° P95 / FPS |
|---|---|---|
| 512×512 | 32.033 ms / 34.557 | 35.205 ms / 29.940 |
| 1024×1024 | 80.360 ms / 12.958 | 105.444 ms / 9.822 |
| 2048×2048 | 282.622 ms / 3.702 | 410.989 ms / 2.514 |

These are Linux characterization, not designated workstation acceptance. The 2048 results do not meet the 33.3 ms/30 FPS target. The separate 2048 median-filter diagnostic has P95 approximately 1254.5 ms; Standard uses Gaussian, so that median row is not part of either full Standard result. The stage measurements identify substantial work in CLAHE, sharpening, scalar mapping/tone passes and orientation. Optimizing those measured paths is the next development priority; native Windows designation/performance/freshness evidence remains required.

The separate normal allocation probe uses the unchanged shared Standard at 64×48. Each identity/nonidentity profile completes 100 warm-ups and 1,000 publication/retention/release cycles, including final measured releases, with zero replacement-new calls, requested bytes and deallocations. Its eight-route positive controls record 8 calls / 150 bytes / 8 releases. These results cover processing of an existing immutable RawFrame; acquisition metadata and excluded allocator routes are not claimed allocation-free.

A separate process using the installed glibc 2.43 debug allocator verifies actual marker/malloc providers, a C/C++/linked-shared positive control (12 successful allocation results, 12 releases, one old/new realloc pair, 26 events, 297 trace-reported bytes), and an empty control. Both 1,000-cycle measured orientation traces contain only Start/End markers and no events. This supplements the portable counters within its documented Linux/glibc scope; no traced timing enters the benchmark and no Windows/private-DLL heap proof follows.

The ordinary candidate generator produces 13 cases / 26 PGM files. Root independently verifies their hashes, canonical big-endian P5 dimensions/payload lengths, seven exact candidate/committed-output byte comparisons, and six provisional backend/full cases with null unapproved thresholds. All 28 original oracle files and derivation hashes remain unchanged. Critical benchmark/count/resource/FPS/provenance/trace arithmetic and seven artifact/schema checks pass independently. All raw JSON, PGMs, trace/control files, logs and QA scripts remain in `out/qa/m08-final/`; `root-artifact-verification.log`, `root-candidate-integrity.log` and `root-schema-validation.log` retain the checks.

## Final source review and platform verification

The whole branch `c0c4102..73eb61f` is independently source/spec approved with no Critical/Important defects. Two nonblocking cleanups are retained: remaining dense evidence-validator/CLI formatting, and the typed basename check does not yet enforce the schema's `.pgm` suffix (actual payload integrity is checked). The original task and fix reviews, including the source-license and reference-approval corrections, remain in the ignored M8 execution records.

At `73eb61f`, [Linux CI](https://github.com/m4bulmagd/Lumora/actions/runs/34197858759) passes 53/53 Debug (73.70 s) and Release (20.98 s), plus X11 smoke in each. [Windows CI](https://github.com/m4bulmagd/Lumora/actions/runs/34197858690) passes 53/53 Debug (69.06 s) and Release (35.70 s). Both platform/configuration artifact bundles contain complete 22-row benchmark and two 20-cycle allocation smoke results; all measured C++ counters are zero. They are smoke, not the normal 1,000-cycle proof. CI provenance correctly reports a dirty checkout for the untracked `vcpkg/` setup directory: its status hash equals the exact porcelain record, and the pinned checkout path is present in the logs. No clean-CI claim is made.

The final Qt environment guard also passes a local incremental Release benchmark build: unset and explicitly empty LD_PRELOAD each complete 22-row smoke; a valid nonempty debug-allocator preload is rejected with exit 5 before creating output or entering measurement. The fresh tests-OFF/Basler-OFF/tools-ON app/tool build and display-environment-free smoke pass, as do the affected Debug/Release evidence/provenance registrations. These later checks do not relabel the normal benchmark's earlier build provenance.

The final documentation supplement is approved and the PR checks pass; integration is recorded below. It does not claim M8 acceptance: designated Windows references/tolerances, 2048 performance and freshness remain open, alongside the separately deferred M4/M5 native checks and M6 hardware entry gate.

## PR integration (2026-09-08)

[PR #12](https://github.com/m4bulmagd/Lumora/pull/12) merged at 07:50:30 UTC as `17d1edee02ffdc51258a507bf14a222fa748b404`. The final source/evidence/documentation review approves `a35030b` with the two disclosed nonblocking source cleanups and no new findings. Both previously pending normal-evidence and local guard conditions are closed.

The complete merged tree equals reviewed PR head `a35030b2bdaad8005774d5a91e1b4dd6803c63fd`. Its [Linux PR run](https://github.com/m4bulmagd/Lumora/actions/runs/34200450036) passes 53/53 Debug (133.89 s) and Release (21.21 s), with X11 1/1 in 0.12/0.04 s. Its [Windows PR run](https://github.com/m4bulmagd/Lumora/actions/runs/34200449995) passes 53/53 Debug (81.29 s) and Release (38.57 s). Both branch-push workflows also pass. Raw logs are retained as `ci-linux-pr12-a35030b.log` and `ci-windows-pr12-a35030b.log` under the final QA directory.

Local `main` was clean and fast-forwarded to the merge. The post-integration status update changes only README/progress/authority/this record; processing source and tests remain identical to the reviewed tree. The normal main workflows perform post-merge verification; [Linux main runs](https://github.com/m4bulmagd/Lumora/actions/workflows/linux-simulator.yml?query=branch%3Amain) and [Windows main runs](https://github.com/m4bulmagd/Lumora/actions/workflows/windows-simulator.yml?query=branch%3Amain) expose their current results. Exact final run IDs/logs are retained in the ignored execution ledger and QA directory, avoiding relabeling the earlier normal benchmark provenance.

The feature branch and worktree, build caches, raw evidence and historical review records are preserved. The next proposed development slice is an exact full-range Window/Level identity copy, measured before broader tone/CLAHE/sharpen/orientation changes. It is a proposal, not implemented performance work. M9 follows measured latency reduction; designated Windows references/tolerances, workstation performance/freshness, M4/M5 native validation and M6 hardware entry remain open.
